{
  "schema": "zero3.commander.command.execution-submit/1.0",
  "command": "execution.submit",
  "protocol_version": "1.0",
  "execution_id": "chatgpt-ocu-budget-patch-20260828T1828Z",
  "commander_id": "chatgpt-web-session",
  "issued_at": "2026-08-28T18:28:00Z",
  "payload": {
    "execution_id": "chatgpt-ocu-budget-patch-20260828T1828Z",
    "capability": "file.patch",
    "args": {
      "diff": "*** Begin Patch\n*** Update File: crates/zero3-providers/src/computer.rs\n@@\n-/// `app` (a running app's name or bundle identifier) is required on every\n-/// variant because it's a required argument on every corresponding real\n-/// `iFurySt/open-codex-computer-use` MCP tool\n-/// (`click`/`type_text`/`press_key`/`get_app_state` all require it) — see\n-/// `OpenComputerUseAdapter` and `docs/ARCHITECTURE.md` §Computer Use.\n+/// `app` (a running app's name or bundle identifier) is required by the\n+/// app-scoped upstream tools. `ListApps` intentionally has no app argument.\n #[derive(Debug, Clone, Serialize, Deserialize)]\n pub enum ComputerAction {\n+    /// Maps to the real `list_apps` MCP tool and is useful as a cheap\n+    /// runtime/session capability probe before app-scoped UIA operations.\n+    ListApps,\n@@\n     fn capabilities(&self) -> Vec<String> {\n*** Update File: crates/zero3-providers/src/open_computer_use.rs\n@@\n     fn capabilities(&self) -> Vec<String> {\n         vec![\n+            \"list_apps\".into(),\n             \"get_app_state\".into(),\n@@\n         let (name, arguments) = match action {\n-            ComputerAction::Screenshot { app } => (\"get_app_state\", json!({ \"app\": app })),\n+            ComputerAction::ListApps => (\"list_apps\", json!({})),\n+            // Keep snapshots bounded. Upstream's cross-platform default is\n+            // 1200 nodes / 64 levels; an agent rarely needs that much state\n+            // for an initial observation, and bounded traversal is materially\n+            // safer on remote/virtualized Windows desktops. Callers can add a\n+            // richer inspect action later without making the default unbounded.\n+            ComputerAction::Screenshot { app } => (\n+                \"get_app_state\",\n+                json!({\n+                    \"app\": app,\n+                    \"max_tree_nodes\": 200,\n+                    \"max_tree_depth\": 16,\n+                    \"text_limit\": 4000\n+                }),\n+            ),\n*** Update File: crates/zero3-providers/tests/open_computer_use_real.rs\n@@\n-    for required in [\"get_app_state\", \"click\", \"type_text\", \"press_key\"] {\n+    for required in [\n+        \"list_apps\",\n+        \"get_app_state\",\n+        \"click\",\n+        \"type_text\",\n+        \"press_key\",\n+    ] {\n@@\n     }\n+\n+    // A real tools/call through the installed Windows runtime, independent\n+    // of deep accessibility traversal. This proves the MCP session can reach\n+    // the native platform runtime before the stronger Notepad state check.\n+    let apps = adapter.execute(ComputerAction::ListApps).await.unwrap();\n+    assert!(apps.ok, \"real list_apps returned MCP error: {:?}\", apps.detail);\n \n     let state = adapter\n*** End Patch"
    }
  }
}